10, 11, -> max => return

def max: fn
  # функция возвращающая максимум двух целочисленных чисел
  params a, b,
  cmp a' ?? b'          # сравнить a' и b'
    >=  a' => return    # если больше или равно
    <   b' => return    # если меньше


# cmp производит сравнение чисел разделённых знаком "??".