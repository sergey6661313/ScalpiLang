10, 11, -> max => return

max'fn
  |функция возвращающая максимум двух целочисленных чисел
  params: a', b',

  if: a' - b'  >  0
    then: a' => return
    else: b' => return

|Ключевое слово if подобно конструкции switch в Си где:
|    then = 1
|    else = 0
|в данном случае знак ">" приведёт к сравнению a' - b' и 0 и вернёт 1 или 0.
|if медленный и заместо него рекомендуется использвовать оперотор сравнения cmp.