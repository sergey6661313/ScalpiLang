fn multiplecate
  param first_arg
  param second_arg

  return first_arg' * second_arg'

fn start 
  -> multiplecate
    6,
    7,

# для выражений строчки с большим отступом так же имеют больший приоритет.