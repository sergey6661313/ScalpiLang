fn multiplecate
  param first_arg
  param second_arg

  return first_arg' * second_arg'

fn start 
  -> multiplecate
    6,
    7,

|в нутри выжарежний блоки имееют больший приоритет 
|и поэтому компилятор вынесет их над блоком 
|и получится как в примере 04_2